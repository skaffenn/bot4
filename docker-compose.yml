version: '3.8'

services:

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"

  bot:
    build:
      context: .
    restart: on-failure
    stop_signal: SIGINT
    depends_on:
        - redis
    command: python main.py


